Itamar Trainin 315425967

Part 1: No Attention

- Model Trainin:
    To train the model please run 'RNN/train.py'. Please use the --help option and specify the relevent configuration. If
    not specified, the default configuration will be used.

    Best model command:
		train.py --embd 50 --hidden 200 --out 500
		
- Test model:
    To test the model please run 'RNN/evaluate.py'. Please use the --help option and specify the relevent configuration.
    Make sure to give the path to the Test source and target data as well as the path to best model. If
    not specified, the default configuration will be used.
